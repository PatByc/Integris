# Decisions Log

---

## DEC-001: Stack selection

Date: 2026-05-09
Status: accepted

Decision: Next.js 15 + TypeScript + Tailwind (frontend), FastAPI + Python 3.12 + SQLAlchemy async + Alembic (backend), Supabase (Postgres + Auth + Storage), Redis + Python workers (queue), Google Cloud Vision (OCR), OpenAI gpt-4.1-mini (AI extraction).

Reason: Supabase provides managed Postgres, Auth (JWT), and Storage in one service, eliminating separate infra. FastAPI async fits the I/O-heavy worker pattern. Next.js App Router enables SSR for initial document list without a separate BFF.

Tradeoffs: Supabase SDK is sync in Python; httpx used directly for async storage uploads. SQLAlchemy async requires asyncpg driver; migrations via Alembic rather than Supabase migrations.

Follow-up: shadcn/ui was planned but dropped — Tailwind utility classes used directly throughout.

---

## DEC-002: AI must never generate FA(3) XML directly

Date: 2026-05-09
Status: accepted

Decision: AI (LLM) is used only for OCR-text → structured JSON extraction. FA(3) XML is generated by a deterministic Python function (`workers/fa3_xml.py`) after human approval.

Reason: KSeF FA(3) XML must be schema-correct and legally binding. LLM output is non-deterministic and cannot be trusted as a source of truth. Any error in submitted XML can result in invoice rejection or compliance issues.

Tradeoffs: More pipeline stages (extract → validate → review → XML generate). But correctness and auditability are non-negotiable.

Follow-up: `generate_fa3_xml()` must be tested for determinism and schema conformance on every change.

---

## DEC-003: Multi-tenant isolation via company_id on every query

Date: 2026-05-09
Status: accepted

Decision: Every document query includes `company_id` from the caller's membership. Cross-company access returns 404, not 403.

Reason: 403 leaks the fact that a document exists. 404 is correct — from the caller's perspective, a document in another company simply does not exist.

Tradeoffs: Slightly obscures permission errors during debugging. Accepted as the correct security posture.

Follow-up: Verified by `tests/test_api.py` multi-tenant tests.

---

## DEC-004: Validation logic lives in workers, mirrored in backend service

Date: 2026-05-09
Status: accepted

Decision: Deterministic validation rules are implemented twice — `workers/validation.py` (dict-based, used by background validation worker) and `backend/app/services/invoice_validation_service.py` (ORM model-based, used after user edits in review UI). Both implement identical rules.

Reason: The workers run in a separate process with no ORM access; they use raw psycopg2 dicts. The backend service uses SQLAlchemy ORM models. Sharing a single implementation would require coupling the two environments.

Tradeoffs: Rules must be kept in sync manually. Mitigated by unit tests covering the dict-based version (which is the worker path).

Follow-up: If rules diverge, the worker version (tested) is authoritative.

---

## DEC-005: Alembic for DB migrations, not Supabase migrations UI

Date: 2026-05-09
Status: accepted

Decision: All schema changes go through Alembic (`backend/alembic/versions/`). Supabase is used only as a managed Postgres host.

Reason: Alembic gives reproducible, version-controlled migrations that run in any environment (local Docker, CI, prod). Supabase's migration UI is a web-only tool.

Tradeoffs: Local dev needs a running Postgres (Docker) to run migrations. Supabase's auto-generated types are not used.

Follow-up: Migration chain: 001 (base schema) → 002 (ksef_upo_polling fields) → 003 (notification_reads table).

---

## DEC-006: Redis job queue with Python workers (no managed queue)

Date: 2026-05-09
Status: accepted

Decision: Background jobs (OCR, extraction, XML generation, KSeF submission, UPO polling) use Redis lpush/brpop via a thin wrapper in `workers/queue.py`. No SQS, Celery, or RQ.

Reason: Minimal operational complexity for MVP. Redis was already needed. Workers are simple polling loops (`BaseWorker`) with 3-retry exponential backoff.

Tradeoffs: No built-in dead-letter queue or visibility timeout. Retries tracked via `retry_count` on the document row. Acceptable for MVP scale.

Follow-up: 7 worker processes started by `scripts/start_dev.ps1`.

---

## DEC-007: KSeF environment gated by KSEF_ENV

Date: 2026-05-09
Status: accepted

Decision: `KSEF_ENV` controls submission behaviour: `dry_run` (no HTTP, fake reference — default), `sandbox` (api-test.ksef.mf.gov.pl/v2), `production` (api.ksef.mf.gov.pl/v2).

Reason: Prevents accidental production submissions during development. Default is `dry_run` so the app works without any KSeF credentials.

Tradeoffs: Sandbox requires KSEF_CLIENT_ID, KSEF_CLIENT_SECRET, KSEF_PUBLIC_KEY_PEM from MF. KSeF sandbox testing not yet done (external dependency).

Follow-up: Production gate requires deliberate env var change + sign-off.

---

## DEC-008: PDF served via backend proxy, not signed Supabase URLs

Date: 2026-05-09
Status: accepted

Decision: The review UI fetches PDFs via `GET /api/v1/documents/{id}/pdf`, which streams the file from Supabase Storage after verifying company ownership. No signed URLs are exposed to the frontend.

Reason: Signed URLs bypass the company_id ownership check — any valid signed URL works regardless of tenant. Proxying through the backend keeps access control in one place.

Tradeoffs: Extra network hop through the backend for PDF bytes. Acceptable for MVP; PDFs are fetched once per review session.

Follow-up: Large PDFs (≤25 MB) stream as a single response. Streaming chunked transfer could be added later.

---

## DEC-009: Document status state machine (14 states)

Date: 2026-05-09
Status: accepted

Decision: Documents move through explicit statuses:
`uploaded → ocr_processing → ocr_failed (retryable)`
`→ extraction_processing → extraction_failed (retryable)`
`→ validation_failed → needs_review`
`→ needs_review → approved → xml_generated`
`→ submission_pending → submitted → accepted (terminal)`
`→ rejected (retryable via retry-submission)`
`→ cancelled (terminal)`

Reason: Explicit statuses make the pipeline observable, retryable at the right point, and auditable. Vague statuses like "processing" hide which stage failed.

Tradeoffs: State transitions must be enforced at the API level (422 for invalid transitions). Tested by `tests/test_api.py` state machine tests.

Follow-up: Status is indexed (`idx_documents_status_orm`) for dashboard filtering.

---

## DEC-010: Test suite — types, scope, and results

Date: 2026-05-14
Status: accepted

Decision: 78 tests across 3 files, all passing. No real DB or Supabase needed to run any test.

**test_validation.py — 45 tests (pure unit)**
- Target: `workers/validation.py:validate_invoice_draft()` and `validate_nip()`
- No external dependencies. Tests run in ~0.1s.
- Covers: all 6 required-field rules, NIP checksum (valid/invalid/length/stripping), totals consistency with ±0.02 tolerance boundary, line-item sum mismatch, future issue date, payment-before-issue-date, all valid VAT rates (0/5/8/23/ZW/NP), invalid VAT rates (numeric and string), multi-line error field paths.
- Known edge case: `"0000000000"` is technically valid by modulo-11 (0%11=0). Not a real NIP but passes the algorithm.

**test_fa3_xml.py — 23 tests (pure unit)**
- Target: `workers/fa3_xml.py:generate_fa3_xml()`
- No external dependencies. Tests run in ~0.1s.
- Covers: output type, XML declaration, parseability, FA3 namespace, KodFormularza attributes, determinism (datetime monkeypatched), seller/buyer fields, P_1/P_2/P_15 elements, P_6 sale date (omit when same as issue, include when different), VAT summary P_13_x/P_14_x (23% and 0%), Adnotacje defaults (P_16–P_18A = "2"), FaWiersz count and NrWierszaFa numbering, amount two-decimal formatting.

**test_api.py — 10 tests (FastAPI TestClient + dep overrides)**
- Target: `backend/app/api/routes/documents.py` routes
- Uses `app.dependency_overrides` for auth/membership/DB. Patches repository functions per test with `unittest.mock.AsyncMock`.
- Covers: unauthenticated requests rejected (401/403), viewer cannot approve (403), cross-company document access returns 404 (GET/DELETE/draft/xml-export), wrong-status transitions rejected 422 (retry-ocr, submit, retry-submission).

Run command: `backend\.venv\Scripts\python.exe -m pytest tests/ -v`
Results: 78 passed, 0 failed (2026-05-14)

Tradeoffs: API tests mock the DB layer — they verify routing/auth/permission logic, not SQL correctness. Integration tests against a real DB are post-MVP scope.

Follow-up: When KSeF sandbox credentials are available, add `tests/test_ksef_sandbox.py`.

---

## DEC-011: Audit trail page — per-document event timeline

Date: 2026-05-14
Status: accepted

Decision: Added `frontend/src/app/audit/[id]/page.tsx` — a read-only server-rendered timeline page showing all audit events for a document. Accessible via "History" link on the review detail page.

Reason: SPEC acceptance criterion #8 ("status and audit trail are visible") was only partially met — document status was shown but there was no UI to see the full event log. Required for compliance (who approved what and when).

Tradeoffs: Server component with two parallel fetches (document + history). No client-side polling — page must be manually refreshed. Acceptable for a logbook use case.

Follow-up: `AuditEventOut` extended with `event_metadata` and `user_id` so the timeline can show error details and attribution. Event label map in the page covers all known event types; unknown types fall back to a formatted version of the raw string.

---

## DEC-012: Avatar dropdown menu — UI scaffold

Date: 2026-05-14
Status: accepted

Decision: Clicking the avatar circle in AppHeader opens a dropdown with: company name + role (top), Settings, Help (middle), Log out (bottom). UI only — no functionality wired yet.

Reason: Users need account context and an exit path from every page. Company name and role are already fetched from `/companies/me`; surfacing them in the menu removes ambiguity about which workspace you are in.

Tradeoffs: Settings, Help, and Log out are currently no-op buttons. Log out, Settings, and Help need to be wired in a follow-up session.

Follow-up: Wired in DEC-013.

---

## DEC-013: Avatar dropdown — Log out, Settings, Help wired

Date: 2026-05-14
Status: accepted

Decision: All three avatar dropdown actions are now functional.
- **Log out**: calls `supabase.auth.signOut()` then `router.push("/")`.
- **Settings** (`/settings`): server-rendered read-only page showing user name, email, role, company name, NIP, account created date, and a KSeF environment note.
- **Help** (`/help`): static server-rendered page with a 5-step workflow walkthrough, 4 FAQs, and a contact email.

Reason: These were the last no-op items in the UI per DEC-012.

Tradeoffs: Settings is read-only for MVP — no editing of company name, NIP, or user profile. Editing requires PATCH endpoints and is post-MVP scope.

Follow-up: Add member management (invite/remove users) to the Settings page in a future phase.
